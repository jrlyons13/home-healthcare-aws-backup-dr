# Phase 5 team review (frozen snapshot)

Team presentation and demo script for **phases 0–5** (through `E2E_DR_SIMULATION=PASS`).

| Artifact | File |
|----------|------|
| PowerPoint | `Home-Healthcare-AWS-Backup-DR-Phase5-Team-Deck.pptx` |
| Regenerate deck | `generate_team_deck.py` |
| Live demo script | [TEAM-DEMO-WALKTHROUGH.md](./TEAM-DEMO-WALKTHROUGH.md) |

## Regenerate (same content, refresh theme/layout only)

From repo root:

```powershell
pip install python-pptx
python docs/presentations/phase5-team-review/generate_team_deck.py
```

Edit **`SLIDES`** in `generate_team_deck.py` only if you intentionally update this Phase 5 snapshot. For Phase 6+, use a copied folder (see parent [README](../README.md)).

Open in PowerPoint → **View → Notes** for speaker notes.
